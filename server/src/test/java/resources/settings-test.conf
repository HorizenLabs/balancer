port = 8080

# set true if we can not interact with a real Native Smart Contract
mockNsc = true

# set true if we can not have rosetta for getting balance
mockRosetta = true

ssl = false

# Native smart contract is reachable via this end points
# nscUrl = http://zendao-test-tn-1.us.horizenlabs.io/dev1/
# nscUrl = http://localhost:8200/
nscUrl = http://zendao-tn-1.de.horizenlabs.io:8200/

# Rosetta instance is running locally (if not mocked)
rosettaUrl = http://localhost:8080/

network = test
# network = main

proposalJsonDataFileName = active_proposal.json
proposalJsonDataPath = src/test/java/resources/

